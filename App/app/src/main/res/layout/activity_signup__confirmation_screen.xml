<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#faf8f9"
    tools:context=".UserActivity.SignupConfirmationScreen">

<!--This is the UI activity for the confirmation screen after the user has
successfully signed up to the app-->
    <ImageView
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_marginBottom="28dp"
        android:background="@drawable/correct"
        android:foregroundTint="@color/teal_700"
        app:layout_constraintBottom_toTopOf="@+id/welcome_text"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="parent"
        app:tint="@color/purple_500"
        android:contentDescription="@string/imgViewContentDesscription" />

    <TextView
        android:id="@+id/welcome_text"
        android:layout_width="307dp"
        android:layout_height="95dp"
        android:layout_marginTop="364dp"
        android:fontFamily="@font/roboto_bold"
        android:text="@string/signup_confirmation_message"
        android:textAlignment="center"
        android:textColor="@color/text_color"
        android:textSize="30sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>