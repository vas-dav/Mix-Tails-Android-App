<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mix_tailsapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/new_logo_1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/new_logo_1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.FullScreen">
        <activity android:name=".DrivingProgress"></activity>
        <activity android:name=".AppWelcomeScreen" />
        <activity android:name=".DrinkRecommendationPage" />
        <activity
            android:name=".SignupConfirmationScreen"
            android:parentActivityName=".SignupActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".QuestionSpinner" />
        <activity android:name=".ChosenDrinkSecondActivity" />
        <activity android:name=".AppLaunching">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>